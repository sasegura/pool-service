import React from 'react';
import type { PoolHealthStatus } from '../types/pool';

export function PoolStatusBadge(_props: {
  status?: PoolHealthStatus;
  className?: string;
  size?: 'sm' | 'md';
}) {
  return null;
}
